# 控制台与隔离测试报告

日期：2026-09-09。Python 3.12.14 / Windows / 项目 `.conda`。

## 本地结果

- 91 项测试通过，包括原有 74 项及新增 17 项控制层/GUI 测试。
- Ruff、pip check、CLI smoke 均通过。
- 原生 Tk 窗口已启动并完成屏幕布局检查：展示默认关闭、指定联系人待校准、白名单禁用。
- 新增测试覆盖默认关闭、类型与 ID 白名单匹配、停止后的写入拒绝、发送中停止竞态、
  窗口句柄变更、白名单存储、配置验证、只读检查不调用模型、GUI 保存和删除。
- 本轮真实微信发送次数为 0；未调用真实模型。

证据见 `docs/test-results/control-panel/`，原始运行记录保留在 `data/wechat-tests/`。
无图形桌面的 Linux CI 会跳过 GUI 测试；不能把该跳过写成 GUI 通过。

## 实机接入阻塞

当前只有微信主窗口，可访问性树为「微信 / Weixin / MMUIRenderSubWindowHW」。
没有所指定联系人的独立窗口，也未获取稳定消息 ID、发送者及所需输入/发送控件。
所以仅完成了真实 UIA 调用接口及测试隔离控制，**尚未完成当前微信客户端的真实收发验收**。

下一步须打开专用联系人独立窗口，验证控件；若仍不暴露必要字段，现有 UIA 接入不可用，
需要另选接入实现。不能只勾选「已校准」来绕过不兼容。
